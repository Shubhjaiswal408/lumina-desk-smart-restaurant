// Copy this to wifi_secrets.h and fill in your own values.
// wifi_secrets.h is gitignored — never commit real credentials.
#define WIFI_SSID     "YourWiFiName"
#define WIFI_PASS     "YourWiFiPassword"
#define MQTT_HOSTNAME "raspberrypi"      // the Pi's mDNS name (without .local)
#define MQTT_BROKER   "192.168.1.10"     // fallback IP if mDNS lookup fails
#define MQTT_PORT     1883
#define TABLE_ID      "07"
